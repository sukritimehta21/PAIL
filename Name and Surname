section .data
msg db 9, 'SUKRITI',10
msglen: equ $ -msg
msg1 db 9, 'MEHTA',10
msg1len: equ $-msg1
section .text
global _start
_start:
    
%macro Write 4
mov eax,%1
mov ebx,%2
mov ecx,%3
mov edx,%4
int 80h
%endmacro

Write 4, 1, msg, msglen
Write 4, 1, msg, msg1len

mov eax, 1
mov ebx, 0
int 80h
